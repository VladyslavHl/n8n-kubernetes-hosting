apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: n8n
spec:
  rules:
  - host: "n8n.ml-eng.dev.euw1.statsperform.cloud"
    http:
      paths:
      - path: "/"
        pathType: ImplementationSpecific
        backend:
          service:
            name: n8n
            port:
              number: 5678
